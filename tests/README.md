# Tests

This directory is reserved for automated tests.

Domain-level tests should arrive first, followed by service orchestration and adapter/infrastructure coverage as the architecture becomes concrete.
