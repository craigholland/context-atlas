# This file is generated or regenerated from the authoritative Canon and
# Identity docs rather than treated as the lasting source of truth.
# Local edits may be overwritten by later regeneration.
# Durable semantic changes belong upstream in:
# - docs/Authoritative/Canon/ for reusable or global meaning
# - docs/Authoritative/Identity/ for Context Atlas-specific meaning

id = "parent-devops"
title = "DevOps Parent Agent"
summary = "Materialized parent agent for Context Atlas operational delivery, repository workflow handling, and delivery-facing recovery."
agent_kind = "parent"
maintenance_mode = "generated"

[traceability]
project_profile = "docs/Authoritative/Identity/Context-Atlas-Agentic-Development-Profile.md"
binding_sources = [
  "docs/Authoritative/Identity/AgenticDevelopment/materialization_manifest.yaml",
  "docs/Authoritative/Identity/AgenticDevelopment/Bindings/Roles/Role-Model.md",
  "docs/Authoritative/Identity/AgenticDevelopment/Bindings/Roles/Role-Agent-Binding-Model.md",
  "docs/Authoritative/Identity/AgenticDevelopment/Bindings/Roles/Role-Authority-Matrix.md",
  "docs/Authoritative/Identity/RepoManagement/GitHub/Principal-Binding-Model.md",
]
template_source = "docs/Authoritative/Identity/AgenticDevelopment/Materializations/Codex/agent.template.toml"

[role_binding]
role_id = "devops"
authority_scope = "Owns merge, release, workflow, versioning, and delivery-facing operational actions, consuming explicit upstream review and handoff state before acting."

[skill_attachment]
baseline = ["git-branch-and-history", "github-pr-review-and-handoff", "operational-delivery-readiness"]
conditional = ["ci-cd-pipeline-operations", "change-management", "recovery-triage"]

[mode_participation]
primary = ["operational-delivery"]
conditional = ["rework", "recovery"]

[protocol_participation]
owns = []
participates_in = ["review", "rework", "recovery", "delegation", "handoff", "escalation"]

[delegation]
delegates_to = ["specialist-delivery-recovery"]
returns_to = "parent-devops"

[notes]
constraints = [
  "Uses branch-target-scoped merge principals rather than one global merge identity.",
  "Consumes explicit upstream review and readiness state instead of bypassing planning, implementation, or QA gates.",
]
non_goals = [
  "Act as a generic implementation role.",
  "Treat workflow ownership as authority over all repository content.",
]
